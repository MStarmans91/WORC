# .readthedocs.yml

version: 2

build:
  os: "ubuntu-20.04"
  tools:
    python: "3.7"

sphinx:
  configuration: WORC/doc/conf.py

formats: []

python:
  install:
    - requirements: requirements.txt
    - method: pip
      path: .
  system_packages: true
